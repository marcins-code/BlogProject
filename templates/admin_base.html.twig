{% extends 'base.html.twig' %}
{% set admin_menu %}
    {{ knp_menu_render('admin', {'template': 'themes/sidebarnemu.html.twig', 'allow_safe_labels': true, 'currentClass': 'uk-active'})|raw }}
{% endset %}

       {% block sidebar_menu %}
           <div class="sidebar-menu uk-card uk-shadow-3d-dark-plus">
               <div class="uk-card-header uk-background-tertiary-full uk-box-shadow-small-b">
                   <h4>Admin menu</h4>
               </div>
               <div class="uk-card-body uk-background-darker">
                   {{ admin_menu }}
               </div>
           </div>
       {% endblock %}

